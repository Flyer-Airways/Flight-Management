spring:
  application:
    name: flight-management
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    mongodb:
      uri: mongodb://mongodb:27017/flight-management
      username: root
      password: secret
kafka:
  bootstrap-server: localhost:9092
  group-id: flight-management-consumer